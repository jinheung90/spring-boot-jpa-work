# ref: https://jojoldu.tistory.com/509
aws:
  paramstore:
    enabled: false
    prefix: /jinheung_project
    name: gateway
    profileSeparator: _

---

spring:
  config:
    activate:
      on-profile: prod

aws:
  paramstore:
    enabled: true
